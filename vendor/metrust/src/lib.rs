pub mod calc;
pub mod constants;
pub mod interpolate;
pub mod io;
pub mod plots;
pub mod projections;
pub mod units;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
