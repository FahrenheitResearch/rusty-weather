[package]
name = "metrust"
version = "0.3.11"
edition = "2021"
description = "A pure-Rust meteorology toolkit with a MetPy-compatible calculation layer"
license = "MIT"

[dependencies]
# Core dependencies
wx-core = { version = "0.3.9", path = "../wx-core" }
wx-math = { version = "0.3.6", path = "../wx-math" }
wx-field = { version = "0.3.6", path = "../wx-field" }
wx-radar = { version = "0.3.6", path = "../wx-radar" }
ecape-rs = { version = "0.1.4", path = "../ecape-rs" }

# Minimal external deps
chrono = "0.4"
rayon = "1"
flate2 = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "calc_bench"
harness = false
