[workspace]
members = [
    "crates/rw-community-relay",
    "crates/rw-community-protocol",
    "crates/rw-glm",
    "crates/rw-gdex",
    "crates/rw-formula",
    "crates/rw-federation-proxy",
    "crates/rw-generation-replication",
    "crates/rw-ingest",
    "crates/rw-odim",
    "crates/rw-query",
    "crates/rw-observations",
    "crates/rw-nexrad-storm",
    "crates/rw-ops-protocol",
    "crates/rw-server",
    "crates/rw-scheduler",
    "crates/rw-sim",
    "crates/rw-sat",
    "crates/rw-sat-check",
    "crates/rw-storm",
    "crates/rw-storm-ml",
    "crates/rw-store",
    "crates/rw-ui",
    "crates/rustwx-calc",
    "crates/rustwx-contour",
    "crates/rustwx-core",
    "crates/rustwx-io",
    "crates/rustwx-models",
    "crates/rustwx-products",
    "crates/rustwx-regrid",
    "crates/rustwx-render",
    "crates/rustwx-sounding",
    "crates/rusty-weather",
    "crates/rusty-weather-ui",
    "vendor/netcrust",
    "vendor/weather-contours",
    "vendor/wrf-rust/crates/wrf-core",
    "vendor/wrf-rust/crates/wrf-formula",
]
exclude = [
    "vendor/ecape-rs",
    "vendor/grib-core",
    "vendor/metrust",
    "vendor/netcrust/vendor/hdf5-reader",
    "vendor/openjp2",
    "vendor/sharprs",
    "vendor/sharppyrs",
    "vendor/wx-core",
    "vendor/wx-field",
    "vendor/wx-radar",
    "vendor/wx-math",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT"
publish = false
rust-version = "1.92"

[workspace.dependencies]
clap = { version = "4.5", features = ["derive"] }
rayon = "1"
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
serde = { version = "1", features = ["derive"] }
# float_roundtrip is required for honest metadata provenance: stored selector
# floats (for example f32-promoted NEXRAD nyquist/elevation values) must
# re-parse to the identical f64, or the store's post-write metadata-equality
# validation rejects its own frames and wedges background followers.
serde_json = { version = "1", features = ["float_roundtrip"] }
thiserror = "2"
ureq = { version = "3", default-features = false, features = ["rustls-no-provider", "rustls-webpki-roots"] }

# netcrust must resolve `hdf5-reader = "0.3"` to the vendored, production-
# tested tree (vendor/netcrust/vendor/hdf5-reader), not the crates.io
# package. Patches only apply from the workspace root, so the pin lives here.
[patch.crates-io]
hdf5-reader = { path = "vendor/netcrust/vendor/hdf5-reader" }
# openjp2 0.6.1 stores a Rust `Layout` inside code blocks allocated and
# zero-initialized by its C-style allocator.  Keep the decoder on the audited
# local patch until that undefined behavior is fixed in a published release.
openjp2 = { path = "vendor/openjp2" }

# The WRF ECAPE golden corpus is pinned to ecape-rs 8292253. Keep that
# algorithm revision while resolving its meteorology support crates to this
# audited workspace tree, avoiding the pinned repository's unused default
# network/TLS feature graph.
[patch."https://github.com/FahrenheitResearch/metrust-py.git"]
metrust = { path = "vendor/metrust" }
wx-math = { path = "vendor/wx-math" }

[profile.release]
lto = "thin"
codegen-units = 1

[profile.release-fast]
inherits = "release"
lto = false
codegen-units = 16
incremental = true
debug = 1
